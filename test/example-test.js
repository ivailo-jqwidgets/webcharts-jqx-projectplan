var describe,
    example,
    it,
    expect;

describe('sumAndDif', function () {
    'use strict';
    var result = example(1, 4);

    it('Should Sum', function () {
        expect(result.sum).toBe(5);
    });
});
